sphinx>=7.0
sphinx-rtd-theme>=2.0
sphinx-gallery>=0.16   # load_style CSS only
nbsphinx>=0.9
numpydoc>=1.7
sphinxcontrib-jquery>=4.1
# Notebook format
jupytext>=1.16
# README.md -> RST conversion: pandoc system binary (not a pip package).
# Install separately: https://pandoc.org/installing.html
# Windows: winget install JohnMacFarlane.Pandoc
# Linux:   apt install pandoc  /  conda install -c conda-forge pandoc
# MRP runtime deps (needed so conf.py can import scripts)
msnoise @ git+https://github.com/ROBelgium/MSNoise.git@master
pooch>=1.8
zstandard>=0.22
